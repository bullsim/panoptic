node_modules
dist
.env
# Vite loads .env, .env.local, .env.<mode> and .env.<mode>.local — every one of
# which conventionally holds developer secrets. Ignore the whole family and
# re-admit only the checked-in template.
.env.*
!.env.example
*.log
screenshots/
qa-shots/
output/
.gev-logs/
.gev-cache/
.DS_Store
.gstack/
3d-models/
